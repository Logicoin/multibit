The MultiLogicSkeleton.app contains the skeleton to produce the MultiLogic Mac application bundle.

To flesh it out it needs:

Everything in the staging directory to be inserted into the:
   MultiLogicSkeleton/Contents/Resources/Java directory

